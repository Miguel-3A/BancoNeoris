apiVersion: v1
kind: Service
metadata:
  name: microservice-cliente-service
  labels:
    name: microservice-cliente-service
    app: microservice
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
  selector:
    app: microservice-cliente
    type: microservice